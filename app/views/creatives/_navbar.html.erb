<nav id="mainNav" class="navbar navbar-default navbar-fixed-top">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <%= link_to "Etf Finance", root_path, class: "navbar-brand page-scroll"  %>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav navbar-right">
                    <li class="<%= 'active' if params[:action] == 'products' %>">
                        <%= link_to "Products", welcome_products_path%>
                    </li>
                    <li class="<%= 'active' if params[:action] == 'services' %>">
                        <%= link_to "Services", welcome_services_path %>
                    </li>
                     <% if params[:controller] == "welcome" && params[:action] == "index" %>
                        <li>
                            <a class="page-scroll" href="#contact">Contact</a>
                        </li>
                    <% else %>
                        <li>
                            <%= link_to "Contact", root_path(:anchor => "contact") %>
                        </li>
                    <% end %>

                    <% if !current_user %>
                        <li class="<%= 'active' if (params[:action] == 'new' && params[:controller] == 'devise/sessions') %>">
                            <%= link_to "Sign-in", new_user_session_path %>
                        </li>
                        <li class="<%= 'active' if (params[:action] == 'new' && params[:controller] == 'registrations') %>">
                            <%= link_to "Sign-up", new_user_registration_path %>
                        </li>
                    <% else %>
                        <li>
                            <%= link_to current_user.email, subscriber_path(current_user.id)%>
                        </li>
                        <li>
                            <%= link_to "Sign out", destroy_user_session_path, method: :delete %>
                        </li>
                    <% end %>
                    <li class="<%= 'active' if params[:controller] == 'chart' %>">
                        <%= link_to "Live Charts", chart_premium_path, class: "btn-primary", id: "live-chart-btn" ,data: { no_turbolink: true }%>
                    </li>
                    
                </ul>
            </div>

        <!-- "controller"=>"welcome", "action"=>"index" -->
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-fluid -->
</nav>


<div class="container container-devise">
	<div class="row centered-form">
	  <div class="col-xs-12 col-sm-8 col-md-4 col-sm-offset-2 col-md-offset-4">
	  	<div class="panel panel-default">
	  		<div class="panel-heading">
	      	<h2 class="panel-title">Log in to access our products</h2>
	      </div>
	      <div class="panel-body">
	       	<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
	      	<form role="form">
	          <div class="form-group">
	            <%= f.input :email, placeholder: 'Email', input_html: { class: 'form-control input-sm' }, required: true, autofocus: true, label: false %>
	          </div>

	          <div class="form-group">
	          	<%= f.input :password, placeholder: "Password ", input_html: { class: 'form-control input-sm' }, required: true, label: false  %>
	           </div>

	          <div class="form-actions">
	           	<%= f.button :submit, "Log in", :class => "btn btn-primary btn-block btn-lg btn-devise" %>
	           </div>

	        </form>
	        <% end %>
	       <%= render "devise/shared/links" %>
	  		</div>
	 		</div>
		</div>
	</div>
</div>


